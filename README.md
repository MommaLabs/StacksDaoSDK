# StacksDao SDK

This repository contains the standalone SDK packages for the StacksDao project.

## Packages

All packages are published to NPM under the `@stacksdao` scope. Find them in the `packages/` directory.

## Development

```bash
npm install
npm run build
```
